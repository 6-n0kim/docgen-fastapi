name: docgen-fastapi
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
      - fastapi
      - uvicorn[standard]
      - pydantic
      - python-multipart
      - requests
      - gunicorn
      - motor             
      - python-dotenv
      - langchain
      - langchain-openai
      - openai
      - openpyxl
      - crewai
      - crewai[tools]